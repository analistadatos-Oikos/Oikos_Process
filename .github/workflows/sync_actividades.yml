name: Sync Actividades Clientify → Oracle

on:
  schedule:
    # Ejecuta todos los días a las 9:00 AM UTC (4:00 AM Colombia)
    - cron: '0 9 * * *'
  workflow_dispatch:  # Permite ejecución manual

jobs:
  sync-actividades:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
    - name: 📥 Checkout código
      uses: actions/checkout@v4
      
    - name: 🐍 Configurar Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'
        
    - name: 📦 Instalar dependencias
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: 🔄 Ejecutar sincronización de actividades
      env:
        CLIENTIFY_API_TOKEN: ${{ secrets.CLIENTIFY_API_TOKEN }}
        ORACLE_USER: ${{ secrets.ORACLE_USER }}
        ORACLE_PASSWORD: ${{ secrets.ORACLE_PASSWORD }}
        ORACLE_DSN: ${{ secrets.ORACLE_DSN }}
      run: python scripts/actividades_oracle.py
      
    - name: 📊 Resultado
      if: always()
      run: echo "Sincronización de actividades completada"
